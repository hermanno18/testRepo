{{ form_start(form) }}
    <div class="form-group">
        {{form_row(form.titre, {'attr': {'class':'form-control'}})}}
    </div>
    <div class="form-group">
        {{form_row(form.content, {'attr': {'class':'form-control'}})}}
    </div>

    {{ form_rest(form) }}
    <button class="btn btn-primary btn-block" onclick="truc()" >{{ button_label|default('Save') }}</button>
    
    <a href="{{ path('article_index') }}" class="btn btn-secondary btn-block">back to list</a>
    {% if button_label is defined %}
        <div class="mt-2">
            {{ include('article/_delete_form.html.twig') }}
        </div>
    {% endif %}
        <div class="mt-2" id="cible">
            
        </div>
  <script lang="javascript" >
  function truc(){
    alert(document.getElementById('article_titre').value)
//    document.getElementById('cible').innerHTML=  
  }
  </script>
    
{{ form_end(form) }}